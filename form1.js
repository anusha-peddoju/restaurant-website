

function validateform()
{
    var name=document.myform.fn.value;

// regular expression that checks if message contains 'programming'
const exp = /^s/;

// check if exp is present in message
let result = name.match(exp);
;
}